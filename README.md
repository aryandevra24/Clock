⌚ Simple Digital Clock
This is a minimalist, real-time digital clock application built using fundamental web technologies: HTML, CSS, and JavaScript.

The project is designed to be straightforward, focusing on displaying the current local time in a clean and accessible format.

✨ Features
Real-Time Update: The clock updates every second to ensure accuracy.

Simple Design: Utilizes CSS Flexbox for centering and basic styling for a modern look.

Pure JavaScript: No external libraries or frameworks are used.

Local Time: Displays the time and date based on the user's local settings using toLocaleString().

🚀 Getting Started
To run this project locally, simply follow these steps:

Clone the repository or download the source code files.

Open the index.html file in your preferred web browser.

The clock will immediately start displaying the current time.

📁 Project Structure
The repository contains three files:

index.html: The main structure of the web page, including the clock's container and linking to the CSS and JavaScript files.

style.css: Contains the styling for the page layout and the clock display.

script.js: The core logic that fetches the current time and updates the display every 1000 milliseconds (1 second).

🛠️ Technologies Used
HTML5

CSS3

JavaScript (ES6+)
